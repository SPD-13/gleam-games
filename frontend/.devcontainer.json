{
    "name": "gleam-games frontend",
    "dockerComposeFile": "../docker-compose.yml",
    "service": "web",
    "workspaceFolder": "/app",
    "customizations": {
        "vscode": {
            "extensions": [
                "gleam.gleam"
            ],
            "settings": {
                "editor.formatOnSave": true
            }
        }
    }
}
