{
    "name": "gleam-games backend",
    "dockerComposeFile": "../docker-compose.yml",
    "service": "server",
    "workspaceFolder": "/app",
    "customizations": {
        "vscode": {
            "extensions": [
                "gleam.gleam"
            ],
            "settings": {
                "editor.formatOnSave": true
            }
        }
    }
}
